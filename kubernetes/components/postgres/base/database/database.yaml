---
apiVersion: postgresql.cnpg.io/v1
kind: Database
metadata:
  name: postgres-${APP}
  namespace: database
spec:
  name: ${APP}
  owner: ${OWNER}
  cluster:
    name: postgres
  extensions: ${EXTENSIONS:=${emptyArray}}
