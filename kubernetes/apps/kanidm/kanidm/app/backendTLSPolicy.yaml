---
# yaml-language-server: $schema=https://schemas.mortebrume.eu/gateway.networking.k8s.io/backendtlspolicy_v1alpha3.json
apiVersion: gateway.networking.k8s.io/v1alpha3
kind: BackendTLSPolicy
metadata:
  name: kanidm-upstream
spec:
  targetRefs:
    - kind: Service
      name: kanidm
      group: ""
  validation:
    caCertificateRefs:
      - kind: Secret
        name: "root-ca"
        group: ""
    hostname: kanidm.kanidm.svc.cluster.local
