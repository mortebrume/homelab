---
# yaml-language-server: $schema=https://schemas.mortebrume.eu/dragonflydb.io/dragonfly_v1alpha1.json
apiVersion: dragonflydb.io/v1alpha1
kind: Dragonfly
metadata:
  name: dragonfly
spec:
  replicas: 2
  args:
    - "--admin_bind=0.0.0.0"
    - --maxmemory=$(MAX_MEMORY)Mi
    - --proactor_threads=2
    - --cluster_mode=emulated
    - --lock_on_hashtags
    - --default_lua_flags=allow-undeclared-keys

  resources:
    requests:
      cpu: 100m
    limits:
      memory: 512Mi

  labels:
    role: master

  networkPolicyEnabled: false # surely one day i'll use proper netpols

  env:
    - name: MAX_MEMORY
      valueFrom:
        resourceFieldRef:
          resource: limits.memory
          divisor: 1Mi
